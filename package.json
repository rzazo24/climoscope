{
  "name": "climoscope",
  "private": true,
  "description": "A minimalist weather app in plain HTML/CSS/JS. This file exists only to run the test suite — the app itself has no build step and no dependencies.",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
